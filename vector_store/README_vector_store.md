# Vector Store

챗봇 실행 시, 웹에서 CV를 업로드해주면 `vector_store` 폴더에 벡터DB가 저장됩니다.

## 저장 구조

```
vector_store/
├── {지원자명1}/
│   └── chroma_db/
└── {지원자명2}/
    └── chroma_db/
```

## 주의사항

- 이 폴더는 벡터DB가 자동으로 생성되는 위치입니다.
- 수동으로 파일을 삭제하거나 수정하지 마세요.
- 벡터DB를 초기화하려면 웹 UI의 "모든 벡터 DB 초기화" 버튼을 사용하세요.

